// Macro-benchmark: Deep recursion and call resolution (Depth 15)
func fibonacci(n: Int) -> Int {
    if n <= 1 {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

func run_fibonacci() {
    let result = fibonacci(15);
}

run_fibonacci();
