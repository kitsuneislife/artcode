struct Pessoa {
    nome: String,
    idade: Int,
}

enum Status {
    Ok,
    Erro(String),
}

let p = Pessoa { nome: "Ada", idade: 42 };
let s1 = Status.Ok;
let s2 = Status.Erro("Falhou!");

println(p.nome)
println(p.idade)
match s2 {
    case .Ok: println("Tudo certo!")
    case .Erro(msg): println(msg)
}