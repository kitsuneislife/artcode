[package]
name = "codegen_js"
version = "0.5.1"
edition = "2024"

[dependencies]
core = { path = "../core" }
reactivity = { path = "../reactivity" }

[dev-dependencies]
lexer = { path = "../lexer" }
parser = { path = "../parser" }
