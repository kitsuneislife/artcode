[package]
name = "codegen_js"
version = "0.5.1"
edition = "2021"

[dependencies]
core = { path = "../core" }
reactivity = { path = "../reactivity" }

[dev-dependencies]
lexer = { path = "../lexer" }
parser = { path = "../parser" }
