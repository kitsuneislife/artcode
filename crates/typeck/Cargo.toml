[package]
name = "typeck"
version = "0.4.0"
edition = "2021"

[dependencies]
core = { path = "../core" }
diagnostics = { path = "../diagnostics" }

[dev-dependencies]
lexer = { path = "../lexer" }
parser = { path = "../parser" }
