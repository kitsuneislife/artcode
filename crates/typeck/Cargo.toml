[package]
name = "typeck"
version = "0.5.1"
edition = "2024"

[dependencies]
core = { path = "../core" }
diagnostics = { path = "../diagnostics" }

[dev-dependencies]
lexer = { path = "../lexer" }
parser = { path = "../parser" }
