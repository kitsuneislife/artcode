// 46_perf_compare_workflow.art
// Exemplo de metadados para rotina de comparacao warmup vs PGO.

let report = "perf.md"
let mode_a = "warmup"
let mode_b = "pgo"

println(f"report={report}")
println(f"compare={mode_a} vs {mode_b}")
println("run: bash scripts/perf_compare.sh")
