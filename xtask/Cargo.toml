[package]
name = "xtask"
version = "0.5.1"
edition = "2021"
# Developer task runner: built from this repository only, never released.
publish = false

[dependencies]
clap = { version = "4", features = ["derive"] }
regex = "1"
